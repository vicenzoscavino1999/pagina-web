# Variables de entorno — copiar a .env.local y completar
# .env.local está en .gitignore y nunca se commitea

# API de tracking (para implementación futura)
# VITE_TRACKING_API_URL=https://api.tudominio.com

# Entorno
# VITE_ENV=development
